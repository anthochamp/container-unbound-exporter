name: Validate & publish (branch)

on:
  push:
    branches: [ main ]

jobs:
  validate:
    uses: anthochamp/dev-toolbox/.github/workflows/container-validate.yml@a3a6cfd24f2ec52cba9f66e6ec9903149ce8408d

  get-version:
    needs: validate
    uses: anthochamp/dev-toolbox/.github/workflows/util-get-latest-git-version-tag.yml@a3a6cfd24f2ec52cba9f66e6ec9903149ce8408d

  pre-publish:
    needs: validate
    uses: ./.github/workflows/unbound-exporter-pre-publish.yml

  publish:
    needs: [validate, get-version, pre-publish]
    permissions:
      packages: write
      id-token: write # used for sigstore/fulcio identity challenge
    uses: anthochamp/dev-toolbox/.github/workflows/container-publish.yml@a3a6cfd24f2ec52cba9f66e6ec9903149ce8408d
    with:
      dockerfile-dir: src
      dhub-readme-file: ./CONTAINER.md
      version: ${{ needs.get-version.outputs.version }}
      container-image-name: ${{ needs.pre-publish.outputs.container-image-name }}
    secrets:
      dhub-username: ${{ secrets.DHUB_USERNAME }}
      dhub-password: ${{ secrets.DHUB_PASSWORD }}
      ghcr-username: ${{ github.repository_owner }}
      ghcr-password: ${{ secrets.GITHUB_TOKEN }}
